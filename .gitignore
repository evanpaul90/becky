node_modules/

# Root-level generated outputs (compile to target project, not committed here)
/CLAUDE.md
/AGENTS.md

# NOTE: dist/ (compiled CLI) IS committed so global installs need no build step.
#       Rebuild it with `npm run build` after editing core/*.ts.

# OS
.DS_Store
.netlify/
tasks/

# personal privacy deny-list — never commit
scripts/.privacy-deny.local
*.tgz
